# stages/readability.yml
version: "1.0"

slug: readability
name: "Readability"
description: readability.md
summary: |
  实现一个程序来计算文本的可读性等级。

category: week-2

languages:
  c:
    starter_path: starters/c/readability
    check_path: checks/c/readability
    files:
      required:
        - readability.c
      allowed:
        - "*.c"
        - "*.h"
      blocked:
        - "*.o"
        - "*.exe"

evaluation:
  timeout: 60

hints:
